const state = {
  
  /**
   *
    name: res.user.displayName,
    img: res.user.photoURL,
    email: res.user.email,
    birth: null,
    sex: "",
    preferredType: "",
    hobby: "",
    comment: "",
   */
}

const mutations = {
}

const getters = {
}

const actions = {
}

export default {
  namespaced:true,
  state,
  mutations,
  getters,
  actions
}
